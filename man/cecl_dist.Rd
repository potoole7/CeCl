% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clust.R
\name{cecl_dist}
\alias{cecl_dist}
\title{Calculate skew-geometric Jensen-Shannon divergence distance matrix
for the conditional extremes model.}
\usage{
cecl_dist(
  dep_obj,
  marg_obj,
  var = NULL,
  laplace_cap = 0.99,
  n_mc = 500,
  ncores = 1,
  par_dist = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{dep_obj}{Object of class \code{cecl_dep}.}

\item{marg_obj}{Object of class \code{cecl_marg}.}

\item{var}{Optional conditioning variable name to calculate distance matrix
for, if not all variables, Default: NULL.}

\item{laplace_cap}{Upper quantile to sample Laplace distribution
truncation point from, Default: 0.99.}

\item{n_mc}{Number of Monte Carlo samples to use in distance
calculation, Default: 500.}

\item{ncores}{Number of cores to use for parallel computation, Default: 1.}

\item{par_dist}{Logical, whether to parallelise the distance computation,
rather than embarrassingly parallel over each variable, which is useful for a
high number of locations, but has significant overheads, Default: FALSE.}

\item{...}{Additional arguments passed to \code{proxy::dist()}.}
}
\value{
List of distance matrices for each variable.
}
\description{
Function to calculate the skew-geometric
Jensen-Shannon Divergence distance matrix for the conditional
extremes model.
}
